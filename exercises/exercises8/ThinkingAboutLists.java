package exercises8;

public class ThinkingAboutLists {
	/*
	 * 1. List<Color> can be a list of colors used to paint a painting
	 * 		(the order of colors applied is important)
	 * 	  List<Account> can be a list of how the bank account changes as the balance changes 
	 * 		(the order is important because it is listed linearly in time)
	 * 2. List<Integer>
	 * 3. List<Boolean> can be used as a list of whether first year students have been approved of course registration
	 * 4. We would like to add elements, delete elements, look at the value of an element, whether the list contain an element
	 * 
	 */
}
